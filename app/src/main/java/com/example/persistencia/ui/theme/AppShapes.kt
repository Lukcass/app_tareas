package com.example.persistencia.ui.theme

import androidx.compose.foundation.shape.CircleShape
import androidx.compose.foundation.shape.RoundedCornerShape
import androidx.compose.ui.unit.dp

/**
 * Formas reutilizables para la nueva interfaz: tarjetas redondeadas,
 * botones tipo "pill" y círculos perfectos para el calendario.
 */
object AppShapes {
    val CardShape = RoundedCornerShape(16.dp)
    val MenuShape = RoundedCornerShape(14.dp)
    val PillShape = RoundedCornerShape(50)
    val RoundButtonShape = CircleShape
    val DayCircleShape = CircleShape
}
